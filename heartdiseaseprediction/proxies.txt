# Add proxy servers here, one per line.
#Talk to joseph about this, see if we should host it on the cloud, or manually enter them
#another thing we have to consider is if we want the agent to be running on the cloud so that 
#my computer doesn't have to be on the whole time, otherwise it would be better to user
# an online platform.


# Example: http://user:pass@ip:port
# Example: socks5://ip:port
